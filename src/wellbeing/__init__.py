"""Multimodal well-being monitoring system for elderly care.

Layer boundaries are enforced by the contracts in :mod:`wellbeing.contracts`.
Only the perception layer touches pixels; every layer above it consumes typed rows.
"""

__version__ = "0.1.0"
